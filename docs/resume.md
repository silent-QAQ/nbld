# 中断恢复说明

本仓库按“可中断、可恢复”方式推进开发。

## 恢复开发入口

优先阅读以下文件：

1. `README.md`
2. `docs/tasks/status.md`
3. `docs/resume.md`

## 当前完成状态

- 阶段 A 已完成
- 阶段 B 进行中
- 服务端已具备最小联通链路：
  - 游客登录
  - 进入世界
  - 玩家移动
  - 查询世界状态
  - SSE 世界事件流
  - WebSocket 世界连接与移动广播
- 历史客户端目录已保留一套团结引擎原型脚本骨架供参考
- 启动脚本已按实例 ID 校验，不会把旧进程误判为新实例
- 客户端路线已统一切换为 H5
- 服务端已具备区块窗口加载与 Rust 区块生成器接入口

## 一键恢复联调

```bash
bash scripts/dev_stack.sh
```

该脚本会：

- 启动最小服务端
- 等待当前实例健康检查通过
- 执行完整接口自检

## 下一个推荐开发点

- 初始化 H5 客户端工程并接入登录/移动链路
- 消费现有 WebSocket 广播并做位置表现
- 增加多玩家广播与基础位置同步协议细化
- 把客户端场景表现切到 `/api/v1/world/chunks`

## 云服务器协同开发

如果 H5 客户端跑在本地浏览器、Go 服务跑在云服务器，优先阅读：

```bash
docs/deployment/cloud-nat.md
```

关键原则：

- 服务监听地址用 `NBLD_GATEWAY_ADDR`
- 云服务器本机自检用 `NBLD_CHECK_BASE_URL`
- H5 客户端填外部可访问的 `Http Base Url`

## 环境限制

- 当前环境尚未初始化 H5 客户端工程
- 当前环境无 `Rust`
